name: Generate API Documentation

on:
  push:
    paths:
      - "api/openapi.yaml"
  pull_request:
    paths:
      - "api/openapi.yaml"

jobs:
  generate-docs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install redoc-cli
        run: npm install -g redoc-cli

      - name: Create docs directory
        run: mkdir -p docs

      - name: Gnosis by Deepgram - Docs
        run: |
          redoc-cli bundle api/openapi.yaml \
            --options.theme.colors.primary.main=#00A89C \
            --options.theme.typography.headings.fontFamily="Inter, sans-serif" \
            --title="Gnosis API Documentation" \
            --output docs/index.html

      - name: Deploy to GitHub Pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
          force_orphan: true
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"
          commit_message: "docs: update API documentation"
